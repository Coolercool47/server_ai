
# 🧠 AI-Generated vs Human-Made Image Classifier Ensemble

Этот проект — исследование и реализация ансамбля моделей глубокого обучения для классификации изображений на **сгенерированные ИИ** и **созданные человеком**. Он построен на базе `PyTorch` и объединяет несколько архитектур: ResNet-50, Swin-T и CLIP, а также использует MLP в роли мета-модели.

## 🔍 Цель проекта

Разработка устойчивой и точной модели, способной отличать:

- ИИ-сгенерированные изображения (Midjourney, DALL·E, Stable Diffusion и др.)
- Изображения, созданные человеком (фотографии, цифровое искусство)

## 🧩 Используемые модели

- `ResNet-50` — мощная сверточная сеть, обученная и дообученная для бинарной классификации.
- `Swin-T` — архитектура на базе Vision Transformer.
- `CLIP` (от OpenAI) — соединяет визуальное представление и текстовое описание.
- `Meta-MLP` — простая полносвязная сеть, обученная на выходах трёх вышеуказанных моделей.

## 🛠️ Архитектура ансамбля

1. Каждое изображение проходит через три независимые модели (`resnet`, `swin`, `clip`).
2. Их вероятностные предсказания объединяются в единый вектор признаков.
3. Этот вектор подаётся на `MetaMLP`, который обучается на основе этих признаков.
4. Финальный класс выбирается по выходу MLP.

## 🏗️ Особенности

- Поддержка обучения и тестирования на `Kaggle` и `Google Colab`.
- Поддержка загрузки моделей из `.pth` и HuggingFace (`transformers`).
- Унифицированные функции `train()` и `test()`.
- Отображение изображений и вероятностей моделей.
- Сохранение и архивация результатов (`.zip`) в конце обучения.

## 📁 Структура проекта

```
server_ai/
├── notebooks/               # Jupyter Notebooks с экспериментами
├── models/                  # Сохранённые модели (.pth)
├── scripts/                 # Основной код инференса и обучения
├── utils/                   # Вспомогательные функции
└── README.md
```

## 🚀 Как начать

1. Установить зависимости:

```bash
pip install -r requirements.txt
```

2. Запустить Jupyter Notebook:

```bash
jupyter notebook
```

3. Использовать `ensemble_inference.ipynb` или `train_ensemble.ipynb` для обучения/тестирования.

## 📦 Примеры использования

```python
from ensemble import predict_image_with_ensemble
img = Image.open("example.jpg")
output = predict_image_with_ensemble(img, resnet, swin, clip_model, clip_processor, meta_model, device)
print(output)
```

## 📊 Результаты

| Model        | Accuracy |
|--------------|----------|
| ResNet-50    | 97%      |
| Swin-T       | 94%      |
| CLIP         | 91%      |
| Meta-MLP     | 🚧 В процессе оптимизации |

## 📌 TODO

- [x] Объединение моделей
- [x] Отображение предсказаний
- [ ] Улучшение мета-модели
- [ ] Добавление поддержки Web-интерфейса
- [ ] Интеграция с Gradio или FastAPI

## 🧠 Автор

[Coolercool47](https://github.com/Coolercool47)
